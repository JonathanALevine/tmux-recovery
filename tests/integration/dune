(executable
 (name autonomy_integration_tests)
 (libraries
  async
  core
  core_unix
  tmux_recovery_adapters
  tmux_recovery_application
  tmux_recovery_domain)
 (preprocess (pps ppx_jane)))

(rule
 (target run_tests)
 (action (run ./autonomy_integration_tests.exe)))
